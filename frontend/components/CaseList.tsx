"use client";import Link from "next/link";import {useEffect,useState} from "react";import {api,QCase} from "@/lib/api";export default function CaseList({base}:{base:string}){const[cases,setCases]=useState<QCase[]>([]);useEffect(()=>{api.list().then(setCases)},[]);return <div className="card">{cases.length===0&&<p className="muted">لا توجد قضايا بعد.</p>}{cases.map(x=><Link href={`${base}/${x.id}`} className="case" key={x.id}><div><b>{x.title}</b><div className="muted">{x.description.slice(0,100)}</div></div><span className="badge">{x.urgency}</span><span className="badge gold">{x.status}</span></Link>)}</div>}