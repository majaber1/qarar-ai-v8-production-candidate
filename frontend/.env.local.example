# Server-side only. The browser never receives the backend key.
QARAR_BACKEND_URL=http://127.0.0.1:8000/api
# Local demo convenience only. Remove in shared/pilot environments and sign in via /login.
QARAR_DEV_AUTO_LOGIN_KEY=qarar-local-dev-key-change-me
