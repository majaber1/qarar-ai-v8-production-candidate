'use client';
import{FormEvent,useState}from'react';import{useRouter}from'next/navigation';import Link from'next/link';import{api}from'@/lib/api';import{useLang}from'@/components/LanguageProvider';
export default function NewProject(){const{t}=useLang(),router=useRouter();const[busy,setBusy]=useState(false),[error,setError]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);try{await api.createProject({name:String(f.get('name')),objective:String(f.get('objective')),owner:String(f.get('owner'))});router.push('/project');router.refresh()}catch{setError(t('تعذر إنشاء المشروع. تأكد من تسجيل الدخول وصلاحيتك.','Could not create the project. Check your session and role.'));setBusy(false)}}return <main className="createFlow container"><div className="breadcrumb"><Link href="/project">{t('مساحة المشغّل','Operator workspace')}</Link><span>/</span>{t('مشروع جديد','New project')}</div><header className="createHeader"><span className="pill blue">{t('مساحة عمل جديدة','New workspace')}</span><h1>{t('أنشئ المشروع أولًا، ثم اجمع ملفاته وقراراته.','Create the project first, then bring its files and decisions together.')}</h1><p>{t('المشروع هو الحاوية التي تجمع الهدف، الملفات، حالات القرار، والمسؤولين في مكان واحد.','A project brings the objective, files, decision cases, and owners into one place.')}</p></header><form className="decisionForm narrowForm" onSubmit={submit}><div className="field"><label>{t('اسم المشروع','Project name')}</label><input name="name" required minLength={3}/></div><div className="field"><label>{t('هدف المشروع','Project objective')}</label><textarea name="objective" required minLength={10}/></div><div className="field"><label>{t('مالك المشروع','Project owner')}</label><input name="owner" required minLength={2}/></div>{error&&<div className="errorBox">{error}</div>}<div className="formActions"><Link className="btn secondary" href="/project">{t('إلغاء','Cancel')}</Link><button className="btn primary" disabled={busy}>{busy?t('جارٍ الإنشاء...','Creating...'):t('إنشاء المشروع','Create project')}</button></div></form></main>}
